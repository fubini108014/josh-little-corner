import { createTheme } from "@mui/material/styles";

export const customTheme = createTheme({
  /*全域自定義Theme設定*/
  custom: {
    primary: {
      //主色
      color: "#B7E5E4", // 藍
    },
    secondary: {
      //次要色
    },
  },
});
